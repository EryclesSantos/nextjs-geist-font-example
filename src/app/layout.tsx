import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Lendas Urbanas - Descubra Histórias e Mitos',
  description: 'Explore o fascinante mundo das lendas urbanas brasileiras e compartilhe suas próprias experiências em nosso fórum interativo.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="pt-BR">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
